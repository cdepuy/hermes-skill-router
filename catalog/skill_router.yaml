name: skill_router
# Pinned to the public release commit. Pin-maturity rule: submit the catalog
# PR only after this commit is >= 2 weeks old (supply-chain policy). Capabilities
# verified against the pinned commit via: hermes plugins doctor skill_router
# (exactly 1 tool: skill_router_route + 1 hook: pre_llm_call).
repo: https://github.com/cdepuy/hermes-skill-router
sha: "9047b1daec79f6bc036c4b8e2898d9e74fd7f407"
subdir: ""
description: >-
  Pre-routes each task to the right skill with a local Laya model (421M
  System-1) before the main model sees it. Reduces sessions "forgetting" to
  load skills. Standalone plugin; user-message injection only (prompt-cache
  safe), fail-open on any error.
maintainer: Chris DePuy
tier: community
requires_hermes: ">=0.19"
docs_url: ""
platforms: [macos, linux]
capabilities:
  provides_tools:
    - skill_router_route
  provides_hooks:
    - pre_llm_call
  provides_middleware: []
  requires_env: []
